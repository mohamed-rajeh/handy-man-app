import '../components/constant/links.dart';

getImageFromServer(String image) {
  return imageLocationLink + image;
}

// String formatDate(DateTime date) {
//  // final formatter = DateFormat('yyyy - MM - d'); // Use 'HH' for 24-hour format
//  // return formatter.format(date);
// }
